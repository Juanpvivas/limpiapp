import 'package:flutter/material.dart';

/// Placeholder — la lógica real de esta pantalla se define en la feature
/// "Mapa de Reportes" (spec separada, ver `docs/ARCHITECTURE.md`).
class ReportMapScreen extends StatelessWidget {
  const ReportMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Próximamente: Mapa de Reportes')),
    );
  }
}
