import 'package:flutter/material.dart';

/// Placeholder — la lógica real de esta pantalla se define en la feature
/// "Mis Reportes" (spec separada, ver `docs/ARCHITECTURE.md`).
class ReportListScreen extends StatelessWidget {
  const ReportListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Próximamente: Mis Reportes')),
    );
  }
}
