import 'package:flutter/material.dart';

/// Placeholder — la lógica real de esta pantalla se define en la feature
/// "Crear Reporte" (spec separada, ver `docs/ARCHITECTURE.md`).
class NewReportScreen extends StatelessWidget {
  const NewReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Próximamente: Crear Reporte')),
    );
  }
}
